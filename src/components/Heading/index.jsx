import { NavLinks } from '../NavLinks';
import * as Styled from './styles';

export function Heading() {
    return (
        <Styled.Container>
            <h1>LINEPIX</h1>
            <NavLinks />
        </Styled.Container>
    );
}
