import { FirstCard } from '../FirstCard';
import * as Styled from './styles';

export const Main = () => {
    return (
        <Styled.Container>
            <FirstCard />
        </Styled.Container>
    );
};
